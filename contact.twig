{% extends "layout.twig" %}

{% block content %}

<div class="bg-glow">

    <!--header-->
    <header>
        <div class="header">
            <div class="container">
                <div class=row>
                    <article class="span2">
                        {{widget('logo', 'name', {"defaultlogo": asset('images/logo.png')})|raw}}
                    </article>
                    <article class="span10">
                        <div class="nav-collapse nav-collapse_ collapse">
                            {{widget('navigation', 'navigation', {'align': 'left'})|raw}}
                        </div>
                    </article>
                </div>
            </div>
        </div>
    </header>

    <div class="global">

        <!--content-->
        <section class="">
            <div class="container">
                <div class="row">
                    <section class="span12">
                    
                        {{widget('space', 'space', {'height': '60'})|raw}}

                        {{widget('map', 'map', {'height': '470', 'zoom': '15', 'address': 'profile'})|raw}}

                        <div class="row divider1">

                            <article class="span3">
                                {{widget('content', 'title1', {content: '<h2>Address</h2>', lines: 'all'})|raw}}
                                {{widget('companyaddress', 'companyaddress', {})|raw}}
                            </article>

                            <article class="span1">

                            </article>

                            <article class="span8">
                                {{zone('main')|raw}}
                            </article>

                        </div>

                    </section>
                </div>
            </div>
        </section>

    </div>

    <!--footer-->
    <footer>
        <div class="container">
            <div class="row">
                <section class="span12">
                    <div class="row">
                        <article class="span12">
                            {{widget('companyname', 'name', {"business": "Chinahi"})|raw}}
                            {{widget('profile', 'copyright', { 'profileType':'copyright' })|raw}}
                            {{widget('space', 'space', {'height': '30'})|raw}}
                        </article>
                    </div>
                </section>
            </div>
        </div>
    </footer>
    
</div>

{% endblock %}